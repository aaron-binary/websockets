const idSchema = {type: 'integer', minimum: 1}

const input = {
  type: 'object',
  properties: {
    id: idSchema,
    userId: idSchema
  }
}

const {compile} = require('./dist/src/index')

function main() {
  compile(input, 'test').then(result => console.log(result))
}

main()
